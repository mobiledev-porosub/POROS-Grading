<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.endrawan.porosgrading">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Admin.DetailActivity"></activity>
        <activity android:name=".User.EditActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".Admin.InsertActionTypeActivity" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Admin.AdminMainActivity" />
        <activity android:name=".User.ReadActivity" />
        <activity android:name=".User.InputActivity" />
        <activity android:name=".User.MainActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity" />
    </application>

</manifest>